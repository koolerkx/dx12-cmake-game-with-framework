### Build log having nose

`Note: including file:` or `メモ: インクルード ファイル` printed in log.

The generator ninja is set to using VSLANG 1033, which means using English to build and print log. Please ensure Visual Studion installed english language pack, otherwise, the default log filter by compiler won't filter these noise.
